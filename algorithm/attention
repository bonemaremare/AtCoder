N=int(input())
S=input()

num=[-1]*N
count=0
for i in range(1,N):
    if S[i]=="E":
        count+=1

num[0]=count

for i in range(1,N):
    numi=num[i-1]
    if S[i-1]=="W":
        numi+=1
    if S[i]=="E":
        numi-=1
    num[i]=numi

print(min(num))
